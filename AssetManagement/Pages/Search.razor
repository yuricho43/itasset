@page "/search"
@using AssetManagement.Entities;
@using AssetManagement.ModelAsset
@using AssetManagement.Services
@using Telerik.DataSource
@using Telerik.DataSource.Extensions
@using Telerik.FontIcons;
@using Telerik.SvgIcons

@inject ILogger<AuthTest> Logger
@inject AssetService svcAsset

<PageTitle>검색</PageTitle>

<PageTitle>FST Digital System</PageTitle>
<div class="card-header text-lg-center pt-2 pb-2 fw-bold px-9"> 자산 검색 </div>

<AuthorizeView>
    <Authorized>

        <TelerikCard>
            <CardHeader Class="page">
            </CardHeader>

            <CardBody>

                <TelerikGrid Class="sbms-grid"
                Data="@gListAsset"
                ConfirmDelete="true"
                Pageable="true"
                Groupable="false"
                Sortable="true"
                Resizable="true"
                Reorderable="true"
                PageSizeChanged="@OnPageSizeChanged"
                OnStateInit="@((GridStateEventArgs<ITAssetMain> args) => OnStateInit(args))"
                PageSize=@gPageSize
                Navigable="true"
                Width="85vw"
                Height="80vh">
                    <GridToolBarTemplate>
                        <GridSearchBox Placeholder="@Placeholder" Width="300px" DebounceDelay="@DebounceDelay" />
                        <span>Write in the textbox on the left-hand side to filter all string columns.</span>
                        <GridCommandButton Command="ExcelExport" Icon="@SvgIcon.FileExcel" ThemeColor="warning">Excel출력</GridCommandButton>
                        <span class="k-toolbar-spacer"></span>
                    </GridToolBarTemplate>
                    <GridSettings>
                        <GridPagerSettings InputType="PagerInputType.Buttons"
                        PageSizes="@gPageList"
                        ButtonCount="20"
                        Position="PagerPosition.Top">
                        </GridPagerSettings>
                    </GridSettings>

                    <GridColumns>
                        <GridColumn Field=@nameof(ITAssetMain.Uuid) Width="90px" Title="고유식별번호" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.AssetNo) Width="60px" Title="자산번호" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.User) Width="70px" Title="사용자" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.MonitorNo) Width="80px" Title="모니터번호" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.MonitorInch) Width="80px" Title="모니터인치" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.MonitorNum) Width="80px" Title="모니터수량" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.Department) Width="110px" Title="부서" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.DeviceType) Width="80px" Title="장비유형" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.DeviceMaker) Width="80px" Title="제조사" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.DeviceModel) Width="150px" Title="모델명" Sortable=true>
                        </GridColumn>
                        <GridColumn Field=@nameof(ITAssetMain.CpuType) Width="100px" Title="CPU" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.RamType) Width="70px" Title="RAM" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.StorageType) Width="80px" Title="저장장치" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.VgaType) Width="140px" Title="VGA" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.WMacAddr) Width="120px" Title="W-MAC" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.DeviceSerial) Width="100px" Title="S/N" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.OsType) Width="90px" Title="운영체제" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.DatePurchase) Width="90px" Title="구입일" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.InstallPlace) Width="110px" Title="설치장소" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.IpAddr) Width="90px" Title="IP주소" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.MacAddr) Width="120px" Title="MAC주소" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.Etc) Width="120px" Title="비고" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.Creator) Width="90px" Title="작성자" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.DateCreate) DisplayFormat="{0:yyyy년 MM월}" Width="90px" Title="작성일" Sortable=true />
                        <GridColumn Field=@nameof(ITAssetMain.DateUpdate) Width="90px" Title="수정일" Sortable=true />
                    </GridColumns>

                </TelerikGrid>


            </CardBody>

        </TelerikCard>


    </Authorized>
    <NotAuthorized>
        <h2>    로그인을 먼저 하세요</h2>
    </NotAuthorized>
</AuthorizeView>

@code {

    //--- Pages
    public List<int?> gPageList = new List<int?>
        {
            10,
            15,
            20,
            30,
            50,
            100,
            500,
            null
        };
    private int gPageSize = 20;

    //--- Search Box
    private string Placeholder { get; set; } = "Search...";
    private int DebounceDelay { get; set; } = 200;

    //--- Main Data
    private List<ITAssetMain> gListAsset = new List<ITAssetMain>();

    protected override async Task OnInitializedAsync()
    {
        SearchAsset();
    }

    private void SearchAsset()
    {
        gListAsset = svcAsset.GetAssetAll();
    }

    private async Task OnPageSizeChanged(int newSize)
    {
        gPageSize = newSize;
    }

    private void OnStateInit(GridStateEventArgs<ITAssetMain> args)
    {

    }

}

<style>
.text-ellipsis {
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
}
</style>
